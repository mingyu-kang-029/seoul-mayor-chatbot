services:
  - type: web
    name: seoul-mayor-chatbot
    runtime: python
    buildCommand: pip install -r requirements-web.txt
    startCommand: uvicorn web2.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
